{
  "name": "pi-minimal-subagent",
  "version": "0.1.0",
  "description": "Minimal Pi extension for session-local background child agents with live activity",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package", "pi-extension", "pi", "subagent", "parallel"],
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "test:process-tree": "node --experimental-strip-types --test test/process-tree.integration.ts",
    "check": "npm test"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
